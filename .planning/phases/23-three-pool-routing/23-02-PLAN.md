---
phase: 23-three-pool-routing
plan: 02
type: tdd
wave: 2
depends_on: ["23-01"]
files_modified:
  - internal/token/picker.go
  - internal/token/picker_test.go
  - internal/token/category.go
  - internal/token/category_test.go
autonomous: true
requirements: [POOL-02]
must_haves:
  truths:
    - "GetPoolForModel 接收 registry 参数，返回 []string 池列表"
    - "pool_floor=basic 的模型返回 [ssoBasic, ssoSuper, ssoHeavy]"
    - "pool_floor=super 的模型返回 [ssoSuper, ssoHeavy]"
    - "pool_floor=heavy 的模型返回 [ssoHeavy]"
    - "PickForModel 按池列表顺序依次尝试，全部无可用 token 时返回错误"
    - "CostForModel 从 registry 读取 quota_default"
    - "旧的双池 fallback 逻辑已移除"
  artifacts:
    - path: "internal/token/picker.go"
      provides: "GetPoolForModel + PickForModel 新实现"
      contains: "PoolLevelFor"
    - path: "internal/token/picker_test.go"
      provides: "三池路由测试"
      contains: "ssoHeavy"
    - path: "internal/token/category.go"
      provides: "CostForModel 从 registry 读取"
      contains: "ResolvedModel"
  key_links:
    - from: "internal/token/picker.go"
      to: "internal/registry/registry.go"
      via: "GetPoolForModel 调用 registry.Resolve"
      pattern: "registry\\.Resolve"
    - from: "internal/token/category.go"
      to: "internal/registry/registry.go"
      via: "CostForModel 调用 registry.Resolve"
      pattern: "registry\\.Resolve"
---

<objective>
重写 picker.go 路由核心：GetPoolForModel 改为基于 registry + pool_floor 的 >= 匹配，PickForModel 按池列表顺序尝试，CostForModel 从 registry 读取。

Purpose: 实现 pool_floor 硬门槛路由，替代旧的 config 驱动双池逻辑
Output: picker.go + category.go 完全重写，旧 fallback 逻辑移除
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-three-pool-routing/23-CONTEXT.md
@.planning/phases/23-three-pool-routing/23-01-SUMMARY.md

<interfaces>
From internal/token/state.go (after Plan 01):
```go
const (
	PoolBasic = "ssoBasic"
	PoolSuper = "ssoSuper"
	PoolHeavy = "ssoHeavy"
)
type PoolLevel int
const (
	PoolLevelBasic PoolLevel = 1
	PoolLevelSuper PoolLevel = 2
	PoolLevelHeavy PoolLevel = 3
)
func PoolLevelFor(pool string) PoolLevel
func PoolNameForLevel(level PoolLevel) string
func AllPoolNames() []string
```

From internal/registry/registry.go:
```go
type ResolvedModel struct {
	RequestName    string
	Family         *store.ModelFamily
	Mode           *store.ModelMode
	EffectiveFloor string // "basic", "super", "heavy"
	UpstreamModel  string
	UpstreamMode   string
}
func (r *ModelRegistry) Resolve(requestName string) (*ResolvedModel, bool)
```

From internal/store/models.go:
```go
type ModelFamily struct {
	PoolFloor     string  `gorm:"size:32;default:basic"`
	QuotaDefault  *string `gorm:"type:text"`
}
type ModelMode struct {
	PoolFloorOverride *string `gorm:"size:32"`
	QuotaOverride     *string `gorm:"type:text"`
}
```

Current picker.go signatures (to be replaced):
```go
func GetPoolForModel(model string, cfg *config.TokenConfig) (string, bool)
func GetPoolsForModel(model string, cfg *config.TokenConfig) (primary, fallback string, ok bool)
func (m *TokenManager) PickForModel(model string, cfg *config.TokenConfig, cat QuotaCategory) (*store.Token, error)
```

Current category.go signatures (to be replaced):
```go
func CostForModel(model string, cfg *config.TokenConfig) int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重写 GetPoolForModel + 移除旧函数 + 测试</name>
  <files>internal/token/picker.go, internal/token/picker_test.go</files>
  <read_first>
    - internal/token/picker.go
    - internal/token/picker_test.go
    - internal/token/state.go
    - internal/registry/registry.go
    - .planning/phases/23-three-pool-routing/23-CONTEXT.md
  </read_first>
  <behavior>
    - GetPoolForModel("grok-3", registry) 当 registry 解析到 pool_floor="basic" 时返回 ["ssoBasic","ssoSuper","ssoHeavy"], true
    - GetPoolForModel("grok-3-super", registry) 当 pool_floor="super" 时返回 ["ssoSuper","ssoHeavy"], true
    - GetPoolForModel("grok-heavy-model", registry) 当 pool_floor="heavy" 时返回 ["ssoHeavy"], true
    - GetPoolForModel("unknown-model", registry) 返回 nil, false
    - GetPoolForModel("", registry) 返回 nil, false
    - PickForModel 按池列表顺序尝试：第一个池无 token 时尝试第二个
    - PickForModel 所有池无 token 时返回 ErrNoTokenAvailable
    - PickForModel 未知模型返回 ErrModelNotFound
  </behavior>
  <action>
    定义接口以解耦 registry 依赖（避免 picker.go 直接 import registry 包）：

    在 picker.go 顶部定义：
    ```go
    // ModelResolver resolves a model request name to pool floor info.
    type ModelResolver interface {
        ResolvePoolFloor(requestName string) (floor string, cost int, ok bool)
    }
    ```

    重写 `GetPoolForModel`：
    ```go
    func GetPoolForModel(model string, resolver ModelResolver) ([]string, bool)
    ```
    - 调用 resolver.ResolvePoolFloor(model) 获取 floor
    - 用 PoolLevelFor(floor) 转为等级
    - 遍历 AllPoolNames()，收集 PoolLevelFor(name) >= floorLevel 的池名 (per D-02, D-05)
    - 返回按等级升序的池列表

    移除 `GetPoolsForModel` 函数（被 GetPoolForModel 的 []string 返回值替代）(per D-06)
    移除 `modelInList` 辅助函数 (per D-14)

    重写 `PickForModel`：
    ```go
    func (m *TokenManager) PickForModel(model string, resolver ModelResolver, cat QuotaCategory) (*store.Token, error)
    ```
    - 调用 GetPoolForModel 获取池列表
    - 按列表顺序依次调用 m.Pick(pool, cat) (per D-12)
    - 第一个成功即返回
    - 全部失败返回 ErrNoTokenAvailable (per D-13)
    - 模型未找到返回 ErrModelNotFound

    重写 picker_test.go：
    - 创建 mockResolver 实现 ModelResolver 接口
    - 测试 basic/super/heavy 三种 floor 的池列表
    - 测试 PickForModel 的顺序尝试和全部失败场景
  </action>
  <verify>
    <automated>cd /Users/easyops/project_ai/grokforge && go test -v -run "TestGetPoolForModel|TestPickForModel" ./internal/token/</automated>
  </verify>
  <acceptance_criteria>
    - picker.go 包含 `type ModelResolver interface`
    - picker.go 包含 `func GetPoolForModel(model string, resolver ModelResolver) ([]string, bool)`
    - picker.go 不包含 `func GetPoolsForModel`
    - picker.go 不包含 `func modelInList`
    - picker.go 不包含 `cfg.BasicModels` 或 `cfg.SuperModels`
    - picker.go 包含 `func (m *TokenManager) PickForModel(model string, resolver ModelResolver, cat QuotaCategory)`
    - picker_test.go 包含 `ssoHeavy`
    - 测试全部通过
  </acceptance_criteria>
  <done>GetPoolForModel 返回 []string 基于 pool_floor >= 匹配，PickForModel 按列表顺序尝试，旧双池逻辑完全移除</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CostForModel 改为从 resolver 读取</name>
  <files>internal/token/category.go, internal/token/category_test.go</files>
  <read_first>
    - internal/token/category.go
    - internal/token/category_test.go
    - internal/token/picker.go (Task 1 产出，获取 ModelResolver 接口)
  </read_first>
  <behavior>
    - CostForModel("grok-3", resolver) 当 resolver 返回 cost=1 时返回 1
    - CostForModel("grok-expert", resolver) 当 resolver 返回 cost=4 时返回 4
    - CostForModel("unknown", resolver) 当 resolver 返回 ok=false 时返回 1 (默认)
    - CostForModel("any", nil) 返回 1 (nil resolver 安全)
  </behavior>
  <action>
    重写 `CostForModel` 签名 (per D-07)：
    ```go
    func CostForModel(model string, resolver ModelResolver) int
    ```
    - 如果 resolver == nil，返回 1
    - 调用 resolver.ResolvePoolFloor(model) 获取 cost
    - 如果 ok=false，返回 1
    - 否则返回 cost

    移除 `ParseModelEntry` 函数（不再需要解析 "model#cost" 格式，cost 来自 registry）。
    注意：如果 ParseModelEntry 在其他地方仍被使用，保留但标记 deprecated。先 grep 确认。

    重写 category_test.go 使用 mockResolver。
  </action>
  <verify>
    <automated>cd /Users/easyops/project_ai/grokforge && go test -v -run "TestCostForModel|TestParseModelEntry" ./internal/token/</automated>
  </verify>
  <acceptance_criteria>
    - category.go 包含 `func CostForModel(model string, resolver ModelResolver) int`
    - category.go 不包含 `cfg.BasicModels` 或 `cfg.SuperModels`
    - category_test.go 不包含 `config.TokenConfig`（除非 ParseModelEntry 保留）
    - 测试全部通过
  </acceptance_criteria>
  <done>CostForModel 从 resolver 读取 cost，不再依赖 config 的模型列表</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → picker | picker 信任 registry 返回的 pool_floor 值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | S (Spoofing) | ModelResolver | accept | resolver 由内部 registry 实现，不接受外部输入 |
| T-23-04 | T (Tampering) | pool_floor | mitigate | store 层已有 validPoolFloors 校验，只允许 basic/super/heavy |
| T-23-05 | D (DoS) | PickForModel | accept | 池列表最多 3 个，遍历开销可忽略 |
</threat_model>

<verification>
- `go test -v ./internal/token/` 全部通过
- picker.go 不包含 `config.TokenConfig` 参数
- category.go 不包含 `config.TokenConfig` 参数
- 旧的 modelInList / GetPoolsForModel 函数已移除
</verification>

<success_criteria>
GetPoolForModel 基于 pool_floor >= 匹配返回池列表，PickForModel 按顺序尝试，CostForModel 从 resolver 读取，所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/23-three-pool-routing/23-02-SUMMARY.md`
</output>
