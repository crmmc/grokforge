---
phase: 23-three-pool-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/token/state.go
  - internal/config/config.go
  - internal/config/defaults.go
  - internal/token/service.go
autonomous: true
requirements: [POOL-01]
must_haves:
  truths:
    - "PoolHeavy 常量存在且值为 ssoHeavy"
    - "PoolLevel 数值常量 basic=1 super=2 heavy=3 可用于比较"
    - "PoolLevelFor 函数可将池名转为等级数值"
    - "heavy_cool_duration_min 配置项可读取"
    - "ReportRateLimit 对 ssoHeavy 池使用 heavy 冷却时间"
  artifacts:
    - path: "internal/token/state.go"
      provides: "PoolHeavy 常量 + PoolLevel 类型 + PoolLevelFor 函数"
      contains: "PoolHeavy"
    - path: "internal/config/config.go"
      provides: "HeavyCoolDurationMin 字段"
      contains: "HeavyCoolDurationMin"
    - path: "internal/token/service.go"
      provides: "ReportRateLimit PoolHeavy 分支"
      contains: "PoolHeavy"
  key_links:
    - from: "internal/token/state.go"
      to: "internal/token/picker.go"
      via: "PoolLevel 常量被路由函数使用"
      pattern: "PoolLevel"
---

<objective>
新增 PoolHeavy 常量、PoolLevel 等级体系、heavy 冷却配置，为 Plan 02 的路由改造提供基础设施。

Purpose: 三池路由的前置条件 — 等级常量和配置项必须先就位
Output: state.go 扩展、config 新增字段、ReportRateLimit 适配
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-three-pool-routing/23-CONTEXT.md

<interfaces>
From internal/token/state.go:
```go
const (
	PoolBasic = "ssoBasic"
	PoolSuper = "ssoSuper"
)
```

From internal/config/config.go (TokenConfig):
```go
type TokenConfig struct {
	BasicCoolDurationMin int `toml:"basic_cool_duration_min"`
	SuperCoolDurationMin int `toml:"super_cool_duration_min"`
	// ... other fields
}
```

From internal/token/service.go (ReportRateLimit):
```go
func (s *TokenService) ReportRateLimit(id uint, reason string) {
	pool := s.manager.GetTokenPool(id)
	var durationMin int
	switch pool {
	case PoolSuper:
		durationMin = s.cfg.SuperCoolDurationMin
	default:
		durationMin = s.cfg.BasicCoolDurationMin
	}
	// ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 PoolHeavy 常量 + PoolLevel 等级体系</name>
  <files>internal/token/state.go, internal/token/state_test.go</files>
  <read_first>
    - internal/token/state.go
    - .planning/phases/23-three-pool-routing/23-CONTEXT.md
  </read_first>
  <behavior>
    - PoolHeavy == "ssoHeavy"
    - PoolLevelBasic == 1, PoolLevelSuper == 2, PoolLevelHeavy == 3
    - PoolLevelFor("ssoBasic") == 1, PoolLevelFor("ssoSuper") == 2, PoolLevelFor("ssoHeavy") == 3
    - PoolLevelFor("basic") == 1, PoolLevelFor("super") == 2, PoolLevelFor("heavy") == 3
    - PoolLevelFor("unknown") == 0
    - PoolNameForLevel(1) == "ssoBasic", PoolNameForLevel(2) == "ssoSuper", PoolNameForLevel(3) == "ssoHeavy"
    - AllPoolNames() returns ["ssoBasic", "ssoSuper", "ssoHeavy"] (升序)
  </behavior>
  <action>
    在 internal/token/state.go 中：
    1. 新增 `PoolHeavy = "ssoHeavy"` 常量，与 PoolBasic/PoolSuper 并列 (per D-03)
    2. 新增 `PoolLevel` 类型 (int)，定义常量 `PoolLevelBasic = 1`, `PoolLevelSuper = 2`, `PoolLevelHeavy = 3` (per D-01)
    3. 新增 `PoolLevelFor(pool string) PoolLevel` 函数，支持池名 ("ssoBasic") 和 floor 名 ("basic") 两种输入
    4. 新增 `PoolNameForLevel(level PoolLevel) string` 函数
    5. 新增 `AllPoolNames() []string` 返回按等级升序的池名列表

    在 internal/token/state_test.go 中编写测试覆盖上述 behavior。
  </action>
  <verify>
    <automated>cd /Users/easyops/project_ai/grokforge && go test -v -run "TestPoolLevel|TestPoolHeavy|TestAllPoolNames" ./internal/token/</automated>
  </verify>
  <acceptance_criteria>
    - state.go 包含 `PoolHeavy = "ssoHeavy"`
    - state.go 包含 `PoolLevelBasic PoolLevel = 1`
    - state.go 包含 `PoolLevelSuper PoolLevel = 2`
    - state.go 包含 `PoolLevelHeavy PoolLevel = 3`
    - state.go 包含 `func PoolLevelFor(pool string) PoolLevel`
    - state_test.go 存在且测试通过
  </acceptance_criteria>
  <done>PoolHeavy 常量和 PoolLevel 等级体系可用，测试全部通过</done>
</task>

<task type="auto">
  <name>Task 2: heavy 冷却配置 + ReportRateLimit 适配</name>
  <files>internal/config/config.go, internal/config/defaults.go, internal/token/service.go</files>
  <read_first>
    - internal/config/config.go (lines 95-115)
    - internal/config/defaults.go (lines 80-104)
    - internal/token/service.go (lines 106-122)
    - config.defaults.toml (lines 120-157)
  </read_first>
  <action>
    1. 在 `config.TokenConfig` struct 中新增 `HeavyCoolDurationMin int \`toml:"heavy_cool_duration_min"\`` 字段，放在 SuperCoolDurationMin 之后 (per D-08)
    2. 在 `config/defaults.go` 的 DefaultConfig() Token 部分新增 `HeavyCoolDurationMin: 60` (heavy 池冷却 60 分钟)
    3. 在 `config.go` 的 `ApplyDBOverrides` switch 中新增 `case "token.heavy_cool_duration_min":` 解析逻辑（参照 basic/super 的 int 解析模式）
    4. 在 `token/service.go` 的 `ReportRateLimit` switch 中新增 `case PoolHeavy:` 分支，使用 `s.cfg.HeavyCoolDurationMin` (per D-09)
    5. 在 `config/runtime.go` 的 clone 逻辑中确认无需额外处理（HeavyCoolDurationMin 是 int，值拷贝即可）
  </action>
  <verify>
    <automated>cd /Users/easyops/project_ai/grokforge && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - config.go TokenConfig 包含 `HeavyCoolDurationMin int`
    - defaults.go 包含 `HeavyCoolDurationMin: 60`
    - service.go ReportRateLimit 包含 `case PoolHeavy:`
    - config.go ApplyDBOverrides 包含 `case "token.heavy_cool_duration_min":`
    - `go build ./...` 成功
  </acceptance_criteria>
  <done>heavy 冷却配置可读取，ReportRateLimit 正确处理 ssoHeavy 池</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → token service | 配置值驱动冷却时间，恶意配置可导致 token 永不冷却或永久冷却 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | T (Tampering) | config.HeavyCoolDurationMin | accept | 与 basic/super 同级别风险，admin 已有 AppKey 认证保护 |
| T-23-02 | D (DoS) | ReportRateLimit | accept | duration=0 时已有 DefaultCoolDuration 兜底（现有逻辑） |
</threat_model>

<verification>
- `go build ./...` 编译通过
- `go test -v ./internal/token/` 全部通过
- state.go 包含 PoolHeavy 和 PoolLevel 定义
</verification>

<success_criteria>
PoolHeavy 常量、PoolLevel 等级体系、heavy 冷却配置全部就位，编译通过，测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/23-three-pool-routing/23-01-SUMMARY.md`
</output>
