---
phase: 23-three-pool-routing
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - internal/config/config.go
  - internal/config/defaults.go
  - internal/config/runtime.go
  - internal/config/runtime_test.go
  - config.defaults.toml
  - internal/flow/chat.go
  - internal/flow/chat_test.go
  - internal/flow/image.go
  - internal/flow/video.go
  - internal/httpapi/openai/provider.go
  - internal/httpapi/openai/models.go
  - internal/httpapi/openai/models_test.go
  - internal/httpapi/admin_config.go
  - internal/httpapi/admin_config_types.go
  - internal/httpapi/admin_config_update.go
  - internal/token/quota.go
  - web/src/lib/validations/config.ts
  - web/src/types/system.ts
  - web/src/app/settings/models-config-form.tsx
autonomous: true
requirements: [POOL-03]
must_haves:
  truths:
    - "BasicModels/SuperModels/PreferredPool 字段从 config struct 中删除"
    - "config.defaults.toml 不包含 basic_models/super_models/preferred_pool"
    - "所有 flow 层调用点使用新的 ModelResolver 签名"
    - "前端设置页面不再显示 basic_models/super_models/preferred_pool"
    - "go build ./... 编译通过"
    - "go test ./... 全部通过"
  artifacts:
    - path: "internal/config/config.go"
      provides: "TokenConfig 无旧字段"
    - path: "config.defaults.toml"
      provides: "配置模板无旧项，有 heavy_cool_duration_min"
      contains: "heavy_cool_duration_min"
    - path: "internal/flow/chat.go"
      provides: "使用 ModelResolver 的 GetPoolForModel"
  key_links:
    - from: "internal/flow/chat.go"
      to: "internal/token/picker.go"
      via: "GetPoolForModel(model, resolver)"
      pattern: "GetPoolForModel"
    - from: "internal/httpapi/openai/provider.go"
      to: "internal/token/picker.go"
      via: "GetPoolForModel 验证模型存在"
      pattern: "GetPoolForModel"
---

<objective>
删除旧配置项 (basic_models/super_models/preferred_pool)，适配所有调用点使用新的 ModelResolver 签名，更新前端。

Purpose: 完成三池路由迁移，消除旧配置的双重真相
Output: 全部调用点适配完成，旧配置彻底清除，编译和测试通过
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-three-pool-routing/23-CONTEXT.md
@.planning/phases/23-three-pool-routing/23-01-SUMMARY.md
@.planning/phases/23-three-pool-routing/23-02-SUMMARY.md

<interfaces>
From internal/token/picker.go (after Plan 02):
```go
type ModelResolver interface {
    ResolvePoolFloor(requestName string) (floor string, cost int, ok bool)
}
func GetPoolForModel(model string, resolver ModelResolver) ([]string, bool)
func (m *TokenManager) PickForModel(model string, resolver ModelResolver, cat QuotaCategory) (*store.Token, error)
```

From internal/token/category.go (after Plan 02):
```go
func CostForModel(model string, resolver ModelResolver) int
```

From internal/registry/registry.go:
```go
type ResolvedModel struct {
    RequestName    string
    Family         *store.ModelFamily
    Mode           *store.ModelMode
    EffectiveFloor string
    UpstreamModel  string
    UpstreamMode   string
}
func (r *ModelRegistry) Resolve(requestName string) (*ResolvedModel, bool)
```

Callers to update:
- flow/chat.go:75 — GetPoolsForModel(req.Model, tokenCfg)
- flow/chat.go:220 — CostForModel(req.Model, f.tokenConfig())
- flow/image.go:253 — GetPoolsForModel(model, tokenCfg)
- flow/video.go:163 — GetPoolsForModel(model, cfg.TokenConfig)
- httpapi/openai/provider.go:56 — GetPoolForModel(model, &cfg.Token)
- httpapi/openai/models.go:65-69 — 遍历 BasicModels/SuperModels 构建模型列表
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除旧配置项 + 清理 config 层</name>
  <files>internal/config/config.go, internal/config/defaults.go, internal/config/runtime.go, internal/config/runtime_test.go, config.defaults.toml, internal/httpapi/admin_config.go, internal/httpapi/admin_config_types.go, internal/httpapi/admin_config_update.go</files>
  <read_first>
    - internal/config/config.go (full file)
    - internal/config/defaults.go
    - internal/config/runtime.go
    - internal/config/runtime_test.go
    - config.defaults.toml
    - internal/httpapi/admin_config.go (lines 90-100)
    - internal/httpapi/admin_config_types.go (lines 80-90, 170-180)
    - internal/httpapi/admin_config_update.go (lines 180-195, 375-390)
  </read_first>
  <action>
    per D-10, D-11 — 直接删除旧配置项，不做向后兼容。

    1. **config.go TokenConfig struct**: 删除 `BasicModels []string`, `SuperModels []string`, `PreferredPool string` 三个字段
    2. **config.go ApplyDBOverrides**: 删除 `case "token.basic_models":`, `case "token.super_models":`, `case "token.preferred_pool":` 三个 case 分支
    3. **defaults.go DefaultConfig()**: 删除 Token 部分的 `BasicModels: [...]`, `SuperModels: [...]`, `PreferredPool: "ssoBasic"` 三行，新增 `HeavyCoolDurationMin: 60`（如果 Plan 01 未添加到 defaults）
    4. **runtime.go**: 删除 `cloned.Token.BasicModels = append(...)` 和 `cloned.Token.SuperModels = append(...)` 两行深拷贝
    5. **runtime_test.go**: 删除所有引用 BasicModels/SuperModels/PreferredPool 的测试代码，或重写为不依赖这些字段
    6. **config.defaults.toml**: 删除 `basic_models = [...]`, `super_models = [...]`, `preferred_pool = "ssoBasic"` 及其注释行，新增 `# Heavy pool cooling duration (minutes)\nheavy_cool_duration_min = 60`
    7. **admin_config_types.go**: 从 TokenConfigResponse 和 TokenConfigUpdate 中删除 BasicModels/SuperModels/PreferredPool 字段
    8. **admin_config.go**: 从 GET 响应构建中删除 BasicModels/SuperModels/PreferredPool 赋值
    9. **admin_config_update.go**: 从 PUT 处理中删除 BasicModels/SuperModels/PreferredPool 的更新逻辑和 DB 写入
  </action>
  <verify>
    <automated>cd /Users/easyops/project_ai/grokforge && go build ./internal/config/ ./internal/httpapi/</automated>
  </verify>
  <acceptance_criteria>
    - config.go 不包含 `BasicModels` 字段
    - config.go 不包含 `SuperModels` 字段
    - config.go 不包含 `PreferredPool` 字段
    - defaults.go 不包含 `BasicModels:`
    - config.defaults.toml 不包含 `basic_models`
    - config.defaults.toml 不包含 `super_models`
    - config.defaults.toml 不包含 `preferred_pool`
    - config.defaults.toml 包含 `heavy_cool_duration_min = 60`
    - admin_config_types.go 不包含 `BasicModels`
    - `go build ./internal/config/ ./internal/httpapi/` 成功（可能有调用点编译错误，Task 2 修复）
  </acceptance_criteria>
  <done>旧配置项从 config struct、defaults、admin API、配置模板中彻底删除</done>
</task>

<task type="auto">
  <name>Task 2: 适配所有调用点 + registry 实现 ModelResolver</name>
  <files>internal/registry/registry.go, internal/flow/chat.go, internal/flow/chat_test.go, internal/flow/image.go, internal/flow/video.go, internal/httpapi/openai/provider.go, internal/httpapi/openai/models.go, internal/httpapi/openai/models_test.go, internal/httpapi/openai/chat_test.go, internal/token/quota.go, internal/token/quota_test.go, web/src/lib/validations/config.ts, web/src/types/system.ts, web/src/app/settings/models-config-form.tsx</files>
  <read_first>
    - internal/registry/registry.go
    - internal/flow/chat.go (lines 60-95, 215-225)
    - internal/flow/image.go (lines 245-265)
    - internal/flow/video.go (lines 155-175)
    - internal/httpapi/openai/provider.go (lines 48-66)
    - internal/httpapi/openai/models.go (lines 60-75)
    - internal/token/quota.go
    - internal/token/picker.go (Plan 02 产出，确认 ModelResolver 接口)
    - web/src/lib/validations/config.ts
    - web/src/types/system.ts (lines 70-80)
    - web/src/app/settings/models-config-form.tsx
  </read_first>
  <action>
    **1. registry 实现 ModelResolver 接口**

    在 internal/registry/registry.go 中新增方法：
    ```go
    // ResolvePoolFloor implements token.ModelResolver.
    func (r *ModelRegistry) ResolvePoolFloor(requestName string) (floor string, cost int, ok bool) {
        rm, found := r.Resolve(requestName)
        if !found {
            return "", 0, false
        }
        cost = 1 // default
        // TODO: parse QuotaDefault/QuotaOverride for cost if needed in Phase 24
        return rm.EffectiveFloor, cost, true
    }
    ```
    这样 *ModelRegistry 自动满足 token.ModelResolver 接口。

    **2. flow 层调用点适配**

    各 flow struct 需要持有 ModelResolver（或 *registry.ModelRegistry）。检查各 flow 的构造函数，添加 resolver 字段。

    - **chat.go**: ChatFlow 需要 resolver 字段。Complete() 中：
      - `GetPoolsForModel(req.Model, tokenCfg)` → `GetPoolForModel(req.Model, f.resolver)` 返回 `pools []string`
      - 将 pools[0] 作为 primary，pools[1:] 作为 fallback 列表传入 executeWithRetry
      - `CostForModel(req.Model, f.tokenConfig())` → `CostForModel(req.Model, f.resolver)`

    - **image.go**: pickTokenForModelExcluding 中：
      - `GetPoolsForModel(model, tokenCfg)` → `GetPoolForModel(model, f.resolver)` 返回 pools
      - 遍历 pools 依次尝试 PickExcluding

    - **video.go**: pickTokenForModel 中：
      - `GetPoolsForModel(model, cfg.TokenConfig)` → `GetPoolForModel(model, f.resolver)` 返回 pools
      - 遍历 pools 依次尝试 Pick

    **3. httpapi 层适配**

    - **provider.go validateModel**: `GetPoolForModel(model, &cfg.Token)` → `GetPoolForModel(model, h.resolver)` 检查返回的 pools 是否非空
    - **models.go**: 不再遍历 BasicModels/SuperModels，改为从 registry.AllEnabled() 构建模型列表

    **4. token/quota.go 适配**
    检查 quota.go 中是否有 CostForModel 调用，如有则更新签名。

    **5. 测试文件适配**
    - chat_test.go: 移除 BasicModels/SuperModels/PreferredPool 引用，使用 mockResolver
    - models_test.go: 同上
    - quota_test.go: 同上

    **6. 前端清理**
    - web/src/types/system.ts: 从 TokenConfig 类型中删除 basic_models/super_models/preferred_pool
    - web/src/lib/validations/config.ts: 从 zod schema 中删除这三个字段
    - web/src/app/settings/models-config-form.tsx: 删除 basicModels/superModels state 和 preferred_pool select，删除相关 UI 区块

    **7. main.go 接线**
    检查 cmd/grokforge/main.go，确保 registry 被传入各 flow 构造函数。
  </action>
  <verify>
    <automated>cd /Users/easyops/project_ai/grokforge && go build ./... && go test ./internal/token/ ./internal/flow/ ./internal/httpapi/...</automated>
  </verify>
  <acceptance_criteria>
    - registry.go 包含 `func (r *ModelRegistry) ResolvePoolFloor`
    - flow/chat.go 不包含 `GetPoolsForModel`
    - flow/chat.go 包含 `GetPoolForModel`
    - flow/image.go 不包含 `GetPoolsForModel`
    - flow/video.go 不包含 `GetPoolsForModel`
    - provider.go 不包含 `&cfg.Token`（在 GetPoolForModel 调用中）
    - models.go 不包含 `cfg.BasicModels`
    - web/src/types/system.ts 不包含 `basic_models`
    - `go build ./...` 成功
    - `go test ./internal/token/ ./internal/flow/ ./internal/httpapi/...` 全部通过
  </acceptance_criteria>
  <done>所有调用点适配完成，前端清理完成，编译和测试全部通过，三池路由完整可用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin API → config | 旧配置项删除后，admin API 不再接受这些字段 |
| frontend → admin API | 前端不再发送旧字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | T (Tampering) | admin config API | accept | 删除字段后 API 自然忽略未知字段（Go JSON 解析默认行为） |
| T-23-07 | I (Info Disclosure) | models.go | accept | AllEnabled() 返回的模型列表与旧行为一致，无额外信息泄露 |
</threat_model>

<verification>
- `go build ./...` 编译通过
- `go test ./...` 全部通过
- grep 确认无残留的 BasicModels/SuperModels/PreferredPool 引用（测试文件除外的 mock）
- config.defaults.toml 包含 heavy_cool_duration_min，不包含旧配置项
</verification>

<success_criteria>
旧配置项彻底删除，所有调用点使用 ModelResolver 接口，前端清理完成，编译和测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/23-three-pool-routing/23-03-SUMMARY.md`
</output>
